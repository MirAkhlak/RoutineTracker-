rootProject.name = "routine_tracker"

// Include other projects if you have a multi-project setup
// include(":app", ":module1", ":module2")

// Additional configuration for plugin management
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}

// Dependency resolution management
dependencyResolutionManagement {
    repositories {
        mavenCentral()
        google()
    }
}
